export class Player {
    constructor(user, ships, battleGrid, shipsGrid, score) {
        this.user = user;
        this.ships = ships;
        this.battleGrid = battleGrid;
        this.shipsGrid = shipsGrid;
        this.score = score;
    }
}